import { ButtonDemo } from '../../../demos/components/ButtonDemo.tsx';
import { HeroContainer } from '../../../components/HeroContainer.tsx';
import { PropsTable } from '../../../components/PropsTable';
import { Surface, Button } from '@micode-ui/react';
import { ArrowRight } from 'phosphor-react';

# Button

A component used to trigger an action or event, such as submitting a form, opening a Dialog, canceling an action, or performing a delete operation.

## Anatomy

Mount all pieces together and create your Button

```tsx
import { Button } from '@micode-ui/react';

<Button.Root>
  <Button.LeftIcon />
  Button Text
  <Button.RightIcon />
</Button.Root>;
```

## Example

<HeroContainer>
  <ButtonDemo />
</HeroContainer>

```tsx
<Button>Hello World!</Button>
```

## Button Variants

Use the `variant` prop to change visual styles of the Button. You can set the value to `primary`, `secondary`, `tertiart` or `danger`.

<HeroContainer>
  <Surface 
    css={{
      display: 'flex',
      gap: '$4',

      '@media (max-width: 768px)': {
        flexWrap: 'wrap',
        width: '80%',
        justifyContent: 'center',
      }
    }}
  >
    <Button variant='primary'>Primary</Button>
    <Button variant='secondary'>Secondary</Button>
    <Button variant='tertiary'>Tertiary</Button>
    <Button variant='danger'>Danger</Button>
  </Surface>
</HeroContainer>

```tsx
<Surface 
  css={{
    display: 'flex',
    gap: '$4'
  }}
>
  <Button variant='primary'>Primary</Button>
  <Button variant='secondary'>Secondary</Button>
  <Button variant='tertiary'>Tertiary</Button>
  <Button variant='danger'>Danger</Button>
</Surface>
```

## Button Sizes

Use the `size` prop to change the size of the button. You can set the value to `sm`, `md`, or `lg`.

<HeroContainer>
  <Surface 
    css={{
      display: 'flex',
      gap: '$4',
      alignItems: 'center'
    }}
  >
    <Button size='sm'>Small</Button>
    <Button size='md'>Medium</Button>
    <Button size='lg'>Large</Button>
  </Surface>
</HeroContainer>

```tsx
<Surface 
  css={{
    display: 'flex',
    gap: '$4',
    alignItems: 'center'
  }}
>
  <Button size='sm'>Small</Button>
  <Button size='md'>Medium</Button>
  <Button size='lg'>Large</Button>
</Surface>
```

## Button States

Button have 3 "states", that you might find useful. They're `loading`, `disabled` and `full`.
You can use them by simply passing the prop as seem in the example.

<HeroContainer>
  <Surface 
    css={{
      display: 'flex',
      gap: '$4',
      width: '80%'
    }}
  >
    <Button loading>Loading</Button>
    <Button disabled>Disabled</Button>
    <Button full>Full</Button>
  </Surface>
</HeroContainer>

```tsx
<Surface 
  css={{
    display: 'flex',
    gap: '$4',
    width: '80%'
  }}
>
  <Button loading>Loading</Button>
  <Button disabled>Disabled</Button>
  <Button full>Full</Button>
</Surface>
```

## API Reference

### Root

Contains all parts of the Button.

<PropsTable
  data={[
    {
      name: 'variant',
      required: false,
      type: '"primary" | "secondary" | "tertiary" | "danger"',
      default: 'primary'
    },
    {
      name: 'size',
      required: false,
      type: '"sm" | "md" | "lg"',
      default: 'md'
    },
    {
      name: 'as',
      description:
        'Render the button as another HTML element, like an anchor or a div',
      required: false,
      typeSimple: 'ElementType',
      type: 'a | div | span | ...'
    },
    {
      name: 'loading',
      required: false,
      type: 'boolean',
      default: 'false'
    },
    {
      name: 'disabled',
      required: false,
      type: 'boolean',
      default: 'false'
    },
    {
      name: 'full',
      required: false,
      type: 'boolean',
      default: 'false'
    }
  ]}
/>

### LeftIcon

A feedback icon that sits on the left side of the button.

<PropsTable
  data={[
    {
      name: 'children',
      required: false,
      type: 'ReactNode'
    }
  ]}
/>

### RightIcon

A feedback icon that sits on the right side of the button.

<PropsTable
  data={[
    {
      name: 'children',
      required: false,
      type: 'ReactNode'
    }
  ]}
/>
