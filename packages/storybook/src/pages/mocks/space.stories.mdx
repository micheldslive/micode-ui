import { Meta } from "@storybook/addon-docs";
import { MocksGrid } from "../../components/MocksGrid";
import { space } from "@micode-ui/mocks";

<Meta title="Mocks/Space" />

# Space

<MocksGrid mocks={space} hasRemValue />
