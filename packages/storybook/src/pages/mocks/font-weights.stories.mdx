import { Meta } from "@storybook/addon-docs";
import { MocksGrid } from "../../components/MocksGrid";
import { fontWeights } from "@micode-ui/mocks";

<Meta title="Mocks/Font Weights" />

# Font Weights

<MocksGrid mocks={fontWeights} />
